# === Ne JAMAIS committer (sensible / local) ===
.credentials.json
*credentials*
.env
*.env
settings.local.json
# État local de Claude Code (ne pas versionner)
sessions/
cache/
shell-snapshots/
session-env/
file-history/
projects/
ide/
plugins/
backups/
stats-cache.json
history.jsonl
.last-cleanup
# .claude/settings.json because of hooks (Ce garde-fou doit suivre le repo, pas l'utilisateur.)
.claude/settings.local.json
# Secrets
.env.*
*.pem
*.key
*.pfx
.npmrc
# OS / éditeur
.DS_Store
Thumbs.db
*.swp
.vscode/
