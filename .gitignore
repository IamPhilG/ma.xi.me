# === Ne JAMAIS committer (sensible / local) ===
.credentials.json
*credentials*
.env
*.env
settings.local.json
# État local de Claude Code (ne pas versionner)
sessions/
cache/
shell-snapshots/
session-env/
file-history/
projects/
ide/
plugins/
backups/
stats-cache.json
history.jsonl
.last-cleanup
# Mémoire et état de session (jamais public)
.claude/memory/
.claude/backups/
# .claude/settings.json because of hooks (Ce garde-fou doit suivre le repo, pas l'utilisateur.)
.claude/settings.local.json
# Doc de chantier ne doit pas être publiée
docs/PLAN-DE-CONSTRUCTION.md
# Secrets
.env.*
*.pem
*.key
*.pfx
.npmrc
# OS / éditeur
.DS_Store
Thumbs.db
*.swp
.vscode/
